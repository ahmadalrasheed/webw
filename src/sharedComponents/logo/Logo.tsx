import React from "react";
import LogoImg from "sharedComponents/assets/logo.svg";

const Logo = () => {
  return (
    <section>
      <img src={LogoImg} alt="logo" />
    </section>
  );
};

export default Logo;
